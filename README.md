# 图片平铺水印制作工具

一个纯前端的图片水印工具，可将多行文本以平铺方式叠加到图片上作为水印。零依赖、无需安装、无需联网，所有处理均在浏览器本地完成，图片不会上传到任何服务器。

## 快速开始

双击 `index.html` 即可在浏览器中打开使用，无需任何构建或安装步骤。

## 功能特性

- **图片上传**：点击选择或拖拽上传，支持 JPG / PNG / WebP
- **实时预览**：调整参数后约 80ms 内自动重绘
- **原始分辨率导出**：预览为等比缩放，导出保持原图尺寸
- **DFT 盲水印**：将不可见文本嵌入频域，可重新提取验证；抗缩放与轻度压缩，不抗裁剪
- **本地处理**：基于 Canvas API，数据不离开本机

### 可调参数

| 参数 | 说明 | 范围 |
|------|------|------|
| 水印文本 | 支持多行，每行独立绘制 | 任意文本 |
| 文本字体 | 微软雅黑、宋体、黑体、楷体、仿宋、Arial 等 10 种 | — |
| 字体大小 | 滑块与数字输入双向同步 | 12–200 px |
| 加粗 | 开关 | — |
| 文本颜色 | 取色器 | 任意颜色 |
| 描边颜色 / 宽度 | 提高水印在复杂背景上的可读性 | 0–10 px |
| 透明度 | 水印整体不透明度 | 0.05–1 |
| 旋转角度 | 水印整体倾斜角度 | -90°–90° |
| 水平 / 垂直间距 | 控制平铺密度 | 0–400 px |
| 导出格式 / 质量 | PNG（无损）或 JPEG（可调质量） | 0.6–1.0 |
| 盲水印启用 | 开关，启用后格式锁定为 PNG | — |
| 盲水印文本 | 嵌入频域的不可见文本 | 最长 64 字符 |
| 嵌入强度 | 空间域波纹幅度（灰度级），越大越鲁棒 | 0.5–10（默认 2） |
| 提取盲水印 | 上传含水印 PNG 提取嵌入文本 | — |

## 使用方法

1. 打开 `index.html`，点击预览区或将图片拖入页面
2. 在右侧面板输入水印文本（可多行）并调整各项参数
3. 预览满意后点击右上角「下载图片」，导出文件自动以 `原文件名_watermark.扩展名` 命名

### 盲水印

- 勾选右侧面板「盲水印」开关，输入标识文本并调整强度
- 启用后导出格式自动锁定为 PNG；点击下载即可获得含水印图片
- 点击顶栏「提取盲水印」选择含水印 PNG，弹窗即显示嵌入的文本
- 盲水印可抵抗轻度缩放与压缩，但**不抗裁剪、旋转或强有损压缩**；同一张图请勿重复嵌入

## 实现原理

1. 将上传图片绘制到与原始分辨率一致的主 Canvas
2. 在离屏 Canvas 中绘制单个水印单元（多行文本 + 间距），测量最宽行确定单元尺寸
3. 通过 `createPattern` 将水印单元作为平铺图案
4. 主画布以图片中心为原点旋转指定角度，填充边长为图片对角线 2 倍的矩形区域，保证任意角度下水印无缝覆盖全图
5. 通过 `canvas.toBlob` 按所选格式与质量导出下载

### 盲水印实现

1. 导出时从主 Canvas 提取亮度通道，padding 到 2 的幂后做 2D DFT（Cooley-Tukey radix-2）
2. 将文本编码为比特序列（帧头 + 长度 + payload + CRC8），通过伪随机生成器在频谱中频环带内选择共轭对称位置
3. 对选定系数实部做 BPSK 调制（bit=1 加 +α，bit=0 加 -α），冗余嵌入后逆变换回空间域，ΔY 通道差值回写 RGB
4. 为保证 file:// 协议下可用，DFT 按行/列分片异步执行（不用 Web Worker），配合进度回调
5. 提取端用相同种子重建位置表，对系数符号多数表决还原比特，校验帧头与 CRC 后解码文本

## 文件结构

```
watermark/
├── index.html   # 页面结构：预览区 + 参数面板 + 提取浮层
├── style.css    # 深色工具风格样式，支持窄屏自适应
├── script.js    # 可见水印逻辑：上传 / 平铺渲染 / 参数联动 / 导出
├── blind.js     # DFT 频域盲水印：嵌入 / 提取（零依赖，纯 JS FFT）
└── README.md
```

## 浏览器兼容性

支持所有现代浏览器（Chrome、Edge、Firefox、Safari 最新版本）。字体选项中的中文字体（微软雅黑、宋体等）依赖操作系统已安装的字体。

## OpenCode对话链接

https://opncd.ai/share/bmWufWw3